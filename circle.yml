machine:
  timezone:
    Europe/London

dependencies:
  cache_directories:
    - "~/.stack"
  override:
    - stack setup
    - stack install hakyll

test:
  override:
    - stack build
  post:
    - rm -rf _site
    - git clone -b cpplondon_feb2018 $CIRCLE_REPOSITORY_URL _site
    - stack exec site build

deployment:
  production:
    branch: cpplondon_feb2018_dev
    commands:
      - git config --global user.email circleci@circleci
      - git config --global user.name CircleCI
      - cd _site && git status
      - cd _site && git add --all
      - cd _site && git commit -m "Update (`date '+%F %T TZ'`) [ci skip]."
      - cd _site && git push origin cpplondon_feb2018

      - git checkout dev
      - git status
      - cd cpplondon_feb2018 && git pull
      - git commit -m "Update cpplondon_feb2018 (`date '+%F %T TZ'`) [ci skip]"
      - git push origin dev

      - git checkout master
      - git status
      - cd cpplondon_feb2018 && git pull
      - git commit -m "Deploy cpplondon_feb2018 (`date '+%F %T TZ'`) [ci skip]"
      - git push origin master
